# Text is normalized to LF in the working tree so that content hashes
# (SHA-256 over file bytes) are stable across machines.
* text=auto eol=lf

*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
