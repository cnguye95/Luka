# toy-repo

A deliberately tiny repository, present so the demo corpus exercises the
repository ingest path. The empty `.luka-repo` file next to this README is what
marks the directory as a single source; without it the files below would each
be ingested on their own.

Selection order is README first, then anything under `docs/`, then the
remaining whitelisted files breadth-first.
